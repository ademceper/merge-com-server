dotnet-api
==========

Minimal .NET 10 minimal API.

Run:

  dotnet run --project services/dotnet-api/Server.Api.csproj

Endpoints:
- GET /        -> greets
- GET /health  -> health check

