{
  "name": "@novu/notification-service",
  "tags": ["type:app"],
  "sourceRoot": "src",
  "projectType": "application",
  "targets": {
    "build": {
      "cache": false,
      "dependsOn": ["^build"],
      "inputs": ["default"],
      "outputs": ["{projectRoot}/src/metadata.ts"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx biome lint ."
      }
    }
  }
}
